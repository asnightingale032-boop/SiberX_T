# This project has no backend and needs no environment variables —
# there is nothing to configure. This file exists only to document
# that fact and to give .gitignore something concrete to allow past
# the blanket .env* ignore rule, per standard practice.
